# 대화 기록 260517-02

---

## [Q] image25~31 분석 및 chapter-3.md 작성

**질의:**
`image25.jpeg`, `image26.jpeg`, `image27.jpeg`, `image28.jpeg`, `image29.jpeg`, `image30.jpeg`, `image31.jpeg`를 분석하고 `docs/book/this-is-ai/chapter-3.md`에 내용을 작성해줘.

**분석 내용:**

**image25 (3.1 프롬프트 템플릿 — 개념):**
- 프롬프트란 LLM에게 작업을 지시하는 명령문
- Spring AI의 `Prompt` 클래스: `Message` + `ChatOption` 구성
- 메시지 타입: `SystemMessage`, `UserMessage`, `AssistantMessage` 역할 설명
- 동적 텍스트(템플릿) 개념 및 `PromptTemplate` 소개 (`{topic}`, `{num}` 자리 표시자)

**image26 (3.1 프롬프트 템플릿 — 코드):**
- `PromptTemplate.builder().template(...).build()` 생성 예제
- `create(Map.of(...))` 로 데이터 바인딩하여 `Prompt` 생성
- `render()` → 텍스트 반환, `createMessage()` → `Message` 객체 반환

**image27 (3.1 — AiServicePromptTemplate 클래스 선언):**
- VS Code에서 `ch03-prompt` 프로젝트 오픈
- `SystemPromptTemplate` 필드 선언 (자리 표시자 없음, 재사용 목적)
- `PromptTemplate` 필드 선언 (`{language}`, `{statement}` 자리 표시자)
- 생성자에서 `ChatClient` 빌드

**image28 (3.1 — promptTemplate1(), promptTemplate2()):**
- `promptTemplate1()`: `userTemplate.create()` → `Prompt` 생성 → `chatClient.prompt(prompt).stream().content()`
- `promptTemplate2()`: `chatClient.prompt().messages(systemTemplate.createMessage(), userTemplate.createMessage(...)).stream().content()`

**image29 (3.1 — promptTemplate3(), promptTemplate4()):**
- `promptTemplate3()`: `render()`로 텍스트 추출 후 `.system()`, `.user()`에 전달
- `promptTemplate4()`: Java `formatted()`로 `%s` 자리 표시자 바인딩

**image30 (3.1 — AiControllerPromptTemplate 및 실행):**
- `@PostMapping("/prompt-template")` 컨트롤러: `promptTemplate1()`~`4()` 주석 하나씩 해제 후 테스트
- `http://localhost:8080`에서 `[prompt-template]` 버튼 클릭하여 확인

**image31 (3.2 복수 메시지 추가 — 개념):**
- Spring AI 번역 결과 화면 (여러 언어 번역 예시)
- 3.2절 시작: LLM 요청 시 `SystemMessage` + 복수 `UserMessage` / `AssistantMessage` 포함 가능
- 대화 기억 유지 목적으로 이전 대화 내용 전체를 프롬프트에 포함 가능
- 대화 기억(Chat Memory) 기능은 09장에서 자세히 다룸

**작성 결과:**
`docs/book/this-is-ai/chapter-3.md`의 `3.1 프롬프트 템플릿` 전체 및 `3.2 복수 메시지 추가` 도입부 작성 완료.

---

## [Q] image32~39 분석 및 chapter-3.md 추가 작성

**질의:**
`image32~35` 분석 후 chapter-3.md 작성, 이어서 `image36~39`도 동일하게 진행.

**분석 내용:**

**image32~33**: 3.2 도입부 재확인 (image31과 동일 내용)

**image34~35 (3.2 — AiServiceMultiMessage 서비스 코드):**
- `multiMessage(String question, List<Message> chatMemory)` 메서드
- `SystemMessage` 생성 후 처음 호출 시 chatMemory에 추가
- `chatClient.prompt().messages(chatMemory).user(question).call().chatResponse()`
- `UserMessage`, `AssistantMessage`를 chatMemory에 저장 후 텍스트 반환

**image35~36 (3.2 — AiControllerMultiMessages 컨트롤러 코드):**
- `@PostMapping("/multi-messages")` 매핑
- `HttpSession`으로 `List<Message>` chatMemory 유지
- 실행: `http://localhost:8080` → [multi-messages] 버튼

**image37 (3.3 — 디폴트 메시지와 옵션):**
- `ChatClient.Builder`의 `defaultSystem()`, `defaultUser()`, `defaultOptions()` 메소드 표
- `AiServiceDefaultMethod`: `defaultSystem()`으로 친절한 시스템 메시지, `defaultOptions(temperature=1.0, maxTokens=300)` 설정

**image38 (3.3 — 서비스 메서드 + 컨트롤러):**
- `defaultMethod()`: `chatClient.prompt().user(question).stream().content()`
- `AiControllerDefaultMethod`: `@PostMapping("/default-method")`, 스트림 응답 반환

**image39 (3.4 — 프롬프트 엔지니어링 가이드라인 표):**
- 7가지 기본 원칙: 명확한 요청, 배경 정보 제공, 간결한 문장, 예시 사용, 단답계 질문 피하기, LLM 한계 이해, 역할 부여

**작성 결과:**
- `3.2 복수 메시지 추가`: AiServiceMultiMessage + AiControllerMultiMessages 코드 + 실행 설명 완료
- `3.3 디폴트 메시지와 옵션`: 표 + AiServiceDefaultMethod + AiControllerDefaultMethod 코드 완료
- `3.4 프롬프트 엔지니어링`: 개념 + 7가지 기본 원칙 표 완료

---

## [Q] image40~42 분석 및 chapter-3.md 추가 작성

**질의:** `image40~42` 분석 후 chapter-3.md에 동일하게 작성.

**분석 내용:**

**image40 (3.5 제로-샷 프롬프트 — 개념 + 서비스 코드 시작):**
- 제로-샷: 예시 없이 작업 수행 요청, LLM이 대규모 학습으로 분류/번역/요약 가능
- `AiServiceZeroShotPrompt`: `{review}` 자리 표시자로 영화 리뷰를 긍정적/부정적/중립적 분류
- 챕터 내 프롬프트 기법 목록: 제로-샷, 퓨-샷, 역할 부여, 스텝-백, 생각의 사슬, 자기 일관성

**image41 (3.5 — 서비스 코드 계속):**
- `ChatClient` 생성자: `temperature(0.0)`, `maxTokens(4)` — 레이블만 반환하므로 결정적 응답 필요
- `zeroShotPrompt()`: `promptTemplate.render()` → `.call().content()`

**image42 (3.5 — 컨트롤러 코드 + 실행 결과):**
- `AiControllerZeroShotPrompt`: `@PostMapping("/zero-shot-prompt")`, 리뷰 감정 레이블 반환
- 실행 결과 화면: 긍정적/부정적/긍정적 레이블 표시

**작성 결과:**
- `3.5 제로-샷 프롬프트`: 개념, 기법 목록, AiServiceZeroShotPrompt + AiControllerZeroShotPrompt 코드 완료

---

## [Q] image43~58 분석 및 chapter-3.md 추가 작성

**질의:** image43~46, 47~49, 50~52, 53~55, 56~58 순차적으로 분석 후 chapter-3.md에 작성.

**분석 내용:**

**image43~45 (3.6 퓨-샷 프롬프트):**
- 개념: 예시 제공으로 원하는 출력 형식 학습, 원-샷(One-Shot) 포함
- `AiServiceFewShotPrompt`: 피자 주문을 JSON으로 변환, `formatted()`로 예시 포함 (`PromptTemplate` 미사용 이유: `{}`가 JSON 표현이기 때문)
- `AiControllerFewShotPrompt`: `@PostMapping("/few-shot-prompt")`, JSON 반환

**image46~47 (3.7 역할 부여 프롬프트):**
- 개념: 전문가/안내자 역할 부여로 스타일·톤·깊이 조정
- `AiServiceRoleAssignmentPrompt`: `.system()`으로 여행 가이드 역할, temperature=1.0, maxTokens=1000, 스트림 응답
- `AiControllerRoleAssignmentPrompt`: `@PostMapping("/role-assignment")`, Flux<String> 반환

**image48~51 (3.8 스텝-백 프롬프트):**
- 개념: 복잡한 질문을 단계별로 분해, 각 단계 답변이 다음 단계 문맥으로 전달
- `stepBackPrompt()`: LLM으로 JSON 배열 형태의 단계별 질문 생성 → ObjectMapper로 파싱 → getStepAnswer() 순차 호출
- `getStepAnswer()`: 이전 단계 답변을 문맥으로 누적해서 LLM 요청
- `AiControllerStepBackPrompt`: `@PostMapping("/step-back-prompt")`, throws Exception

**image52~54 (3.9 생각의 사슬 프롬프트):**
- 개념: "한 걸음씩 생각해 봅시다(Let's think step by step)" 문구로 중간 추론 단계 유도
- `AiServiceChainOfThoughtPrompt`: `.user()`에 CoT 문구 + 질문 포함, 스트림 응답
- `AiControllerChainOfThoughtPrompt`: `@PostMapping("/chain-of-thought")`, Flux<String> 반환

**image55~58 (3.10 자기 일관성):**
- 개념: LLM에 5번 요청 → 다수결로 최종 응답 결정, 출력 분산성 해결
- `AiServiceSelfConsistency`: PromptTemplate(`{content}`) + 5번 루프(temperature=1.0) + importantCount/notImportantCount 집계
- `AiControllerSelfConsistency`: `@PostMapping("/self-consistency")`, 텍스트 반환

**작성 결과:**
- `3.6 퓨-샷 프롬프트`: 개념 + 코드(서비스, 컨트롤러) 완료
- `3.7 역할 부여 프롬프트`: 개념 + 코드(서비스, 컨트롤러) 완료
- `3.8 스텝-백 프롬프트`: 개념 + stepBackPrompt() + getStepAnswer() + 컨트롤러 완료
- `3.9 생각의 사슬 프롬프트`: 개념 + 코드(서비스, 컨트롤러) 완료
- `3.10 자기 일관성`: 개념 + 코드(서비스, 컨트롤러) 완료 → chapter-3.md 전체 완성